# Spring Boot API base path, including the /xzm context path.
# Copy this file to .env for local development.
# Keep it relative so Vite can proxy locally and Nginx can proxy in production.
VITE_API_BASE=/xzm
